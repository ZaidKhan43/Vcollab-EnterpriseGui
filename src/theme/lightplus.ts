
import appTheme from './index';

const lightModePuls = {
    ...appTheme,
    palette: {
      primary: {
        dark:'rgba(0,255,0,1.0)',
        main: '#ff0000',
      },
      action:{
        selected: 'rgba(255, 0, 0,0.5)',
        hover: 'rgba(255,0,0,0.1)'
      }
    },

  } 

 export default lightModePuls;