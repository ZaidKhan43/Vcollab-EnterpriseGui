import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    sideBarFooter : {
        position: 'absolute',
        left: 0,
        bottom: 0,
        //backgroundColor: 'red',
        color: 'white',
        textAlign: 'center',
        height: 'auto',
        width : '100%'
    }
}));